import react from react
import statelessComponent from "../higher_order_components/statelessComponent"



const NavBar = (props) => {
   <statelessComponent>
      <span class={styles.NavBarItem} ></span>
   </statelessComponent>
}