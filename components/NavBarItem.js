import react from 'react';

import styles from "./NavBarItem.css"
const NavBarItem.js = (props) => {
   <statelessComponent>
      <span className={styles.NavBarItem}></span>
   </statelessComponent>
}